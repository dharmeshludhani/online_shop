pipeline{
    
    agent {label "Dev"};
    
    stages{
        stage("code Clone"){
            steps{
                git url:"https://github.com/dharmeshludhani/online_shop.git", branch: "jenkins"
            }
        }
        stage("Code Build"){
            steps{
                echo "code build by  docker-compose and will be deployed on Jenkins Agent"
            }
        }
        stage("Testing Phase"){
            steps{
                echo "Data will be available soon"
            }
        }
        stage("Deployment"){
            steps{
                sh "docker compose down"
                sh "docker compose up -d"
            }
        }
    
    }
}
